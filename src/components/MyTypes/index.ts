export type IChildren = {
    children:object;
}
export type IButton = {
    txt:string;
    still:string;
}
export type IIcon = {
    children:React.ReactNode;
    link:string;
}
export type IGames = {
    img:string;
    followers:string;
}
export interface ICard{
    txt:string;
    children:React.ReactNode;
}